<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jeovanimartinez.androidutils">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application>

        <activity
            android:name=".about.AboutActivity"
            android:theme="@style/AndroidUtilsTheme.Translucent" />

        <activity
            android:name=".reviews.RateAppActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AndroidUtilsTheme.Translucent" />

        <!-- Receiver for ShareUtils -->
        <receiver android:name=".share.ApplicationSelectorReceiver" />

    </application>

</manifest>

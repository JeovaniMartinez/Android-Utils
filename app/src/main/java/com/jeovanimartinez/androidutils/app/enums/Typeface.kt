package com.jeovanimartinez.androidutils.app.enums

/**
 * Enum for typeface (font) to be used for drawing text watermarks.
 * */
enum class TextWatermarkTypeface {
    DEFAULT,
    BALLET,
    FUGAZ_ONE,
    KARLA,
    OI,
}

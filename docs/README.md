The documentation are generated by dokka and is published in gh-pages, this folder contains only a backup from the documentation, and the file is stored in RAR format to prevent that GitHub include the files in the languages statistics.